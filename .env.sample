# PostgreSQL (local dev)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=rails_book_app_development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Rails (used in development)
SECRET_KEY_BASE=dev-secret

# Optional: for production or Supabase connection
# DATABASE_URL=postgresql://your_user:your_password@host:port/database
# In production (e.g. Render), set DATABASE_URL and RAILS_MASTER_KEY in your environment.